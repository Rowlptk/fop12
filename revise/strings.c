//Strings
//
//==> char array;
//==>  strings

// string functions


#include<stdio.h>
#include<string.h>

void main() {
    /*char str[10] = "Welcome";
    gets(str); // scanf
    puts(str);
    printf("The String Content is : %s", str);
    */
    char str1[20], str2[20];
    printf("Enter string 1: ");
    gets(str1);
    printf("Enter string 2: ");
    gets(str2);

    /*strupr(str1);
    puts(str1);
    strlwr(str2);
    puts(str2);

    strrev(str1);
    puts(str1);
*/
    //strcpy(str1, str2);
    //puts(str1);
    //puts(str2);

   // printf("The length of String is %d", strlen(str1));
    //printf("%d", strcmp(str1, str2));





}
